<%= form_for(@set_compilation_link) do |f| %>
  <% if @set_compilation_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_compilation_link.errors.count, "error") %> prohibited this set_compilation_link from being saved:</h2>

      <ul>
      <% @set_compilation_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :set_id %><br>
    <%= f.number_field :set_id %>
  </div>
  <div class="field">
    <%= f.label :setcompilation_id %><br>
    <%= f.number_field :setcompilation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
