<%= form_for(@session_answer) do |f| %>
  <% if @session_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@session_answer.errors.count, "error") %> prohibited this session_answer from being saved:</h2>

      <ul>
      <% @session_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :session_id %><br>
    <%= f.number_field :session_id %>
  </div>
  <div class="field">
    <%= f.label :answer_id %><br>
    <%= f.number_field :answer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
