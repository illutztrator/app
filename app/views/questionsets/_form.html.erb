<%= form_for(@questionset) do |f| %>
  <% if @questionset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questionset.errors.count, "error") %> prohibited this questionset from being saved:</h2>

      <ul>
      <% @questionset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
