<%= form_for(@questiongroup) do |f| %>
  <% if @questiongroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@questiongroup.errors.count, "error") %> prohibited this questiongroup from being saved:</h2>

      <ul>
      <% @questiongroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
